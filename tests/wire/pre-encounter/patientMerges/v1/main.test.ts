// This file was auto-generated by Fern from our API Definition.

import * as CandidApi from "../../../../../src/api/index";
import { CandidApiClient } from "../../../../../src/Client";
import { mockServerPool } from "../../../../mock-server/MockServerPool";
import { mockOAuthScheme } from "../../../mockAuth";

describe("V1Client", () => {
    test("create", async () => {
        const server = mockServerPool.createServer();
        mockOAuthScheme(server);

        const client = new CandidApiClient({
            maxRetries: 0,
            clientId: "YOUR_CLIENT_ID",
            clientSecret: "YOUR_CLIENT_SECRET",
            environment: { candidApi: server.baseUrl, preEncounter: server.baseUrl },
        });
        const rawRequestBody = {
            alternative_patient_mrn: "alternative_patient_mrn",
            primary_patient_mrn: "primary_patient_mrn",
        };
        const rawResponseBody = {
            id: "id",
            organization_id: "organization_id",
            deactivated: true,
            version: 1,
            updated_at: "2024-01-15T09:30:00Z",
            updating_user_id: "updating_user_id",
            alternative_patient_mrn: "alternative_patient_mrn",
            primary_patient_mrn: "primary_patient_mrn",
        };

        server
            .mockEndpoint()
            .post("/patient-merge/v1")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.preEncounter.patientMerges.v1.create({
            alternativePatientMrn: "alternative_patient_mrn",
            primaryPatientMrn: "primary_patient_mrn",
        });
        expect(response).toEqual({
            body: {
                id: CandidApi.preEncounter.PatientMergeId("id"),
                organizationId: CandidApi.preEncounter.OrganizationId("organization_id"),
                deactivated: true,
                version: 1,
                updatedAt: new Date("2024-01-15T09:30:00.000Z"),
                updatingUserId: CandidApi.preEncounter.UserId("updating_user_id"),
                alternativePatientMrn: "alternative_patient_mrn",
                primaryPatientMrn: "primary_patient_mrn",
            },
            ok: true,
            headers: expect.any(Object),
            rawResponse: expect.any(Object),
        });
    });

    test("get_status", async () => {
        const server = mockServerPool.createServer();
        mockOAuthScheme(server);

        const client = new CandidApiClient({
            maxRetries: 0,
            clientId: "YOUR_CLIENT_ID",
            clientSecret: "YOUR_CLIENT_SECRET",
            environment: { candidApi: server.baseUrl, preEncounter: server.baseUrl },
        });

        const rawResponseBody = { merge_status: "none" };

        server
            .mockEndpoint()
            .get("/patient-merge/v1/status/mrn_or_id")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.preEncounter.patientMerges.v1.getStatus("mrn_or_id");
        expect(response).toEqual({
            body: {
                mergeStatus: "none",
            },
            ok: true,
            headers: expect.any(Object),
            rawResponse: expect.any(Object),
        });
    });

    test("get_all_by_mrn", async () => {
        const server = mockServerPool.createServer();
        mockOAuthScheme(server);

        const client = new CandidApiClient({
            maxRetries: 0,
            clientId: "YOUR_CLIENT_ID",
            clientSecret: "YOUR_CLIENT_SECRET",
            environment: { candidApi: server.baseUrl, preEncounter: server.baseUrl },
        });

        const rawResponseBody = [
            {
                id: "id",
                organization_id: "organization_id",
                deactivated: true,
                version: 1,
                updated_at: "2024-01-15T09:30:00Z",
                updating_user_id: "updating_user_id",
                alternative_patient_mrn: "alternative_patient_mrn",
                primary_patient_mrn: "primary_patient_mrn",
            },
            {
                id: "id",
                organization_id: "organization_id",
                deactivated: true,
                version: 1,
                updated_at: "2024-01-15T09:30:00Z",
                updating_user_id: "updating_user_id",
                alternative_patient_mrn: "alternative_patient_mrn",
                primary_patient_mrn: "primary_patient_mrn",
            },
        ];

        server
            .mockEndpoint()
            .get("/patient-merge/v1/all/mrn")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.preEncounter.patientMerges.v1.getAllByMrn("mrn");
        expect(response).toEqual({
            body: [
                {
                    id: CandidApi.preEncounter.PatientMergeId("id"),
                    organizationId: CandidApi.preEncounter.OrganizationId("organization_id"),
                    deactivated: true,
                    version: 1,
                    updatedAt: new Date("2024-01-15T09:30:00.000Z"),
                    updatingUserId: CandidApi.preEncounter.UserId("updating_user_id"),
                    alternativePatientMrn: "alternative_patient_mrn",
                    primaryPatientMrn: "primary_patient_mrn",
                },
                {
                    id: CandidApi.preEncounter.PatientMergeId("id"),
                    organizationId: CandidApi.preEncounter.OrganizationId("organization_id"),
                    deactivated: true,
                    version: 1,
                    updatedAt: new Date("2024-01-15T09:30:00.000Z"),
                    updatingUserId: CandidApi.preEncounter.UserId("updating_user_id"),
                    alternativePatientMrn: "alternative_patient_mrn",
                    primaryPatientMrn: "primary_patient_mrn",
                },
            ],
            ok: true,
            headers: expect.any(Object),
            rawResponse: expect.any(Object),
        });
    });

    test("deactivate", async () => {
        const server = mockServerPool.createServer();
        mockOAuthScheme(server);

        const client = new CandidApiClient({
            maxRetries: 0,
            clientId: "YOUR_CLIENT_ID",
            clientSecret: "YOUR_CLIENT_SECRET",
            environment: { candidApi: server.baseUrl, preEncounter: server.baseUrl },
        });

        server.mockEndpoint().delete("/patient-merge/v1/id/version").respondWith().statusCode(200).build();

        const response = await client.preEncounter.patientMerges.v1.deactivate(
            CandidApi.preEncounter.PatientMergeId("id"),
            "version",
        );
        expect(response).toEqual({
            body: undefined,
            ok: true,
            headers: expect.any(Object),
            rawResponse: expect.any(Object),
        });
    });

    test("scan", async () => {
        const server = mockServerPool.createServer();
        mockOAuthScheme(server);

        const client = new CandidApiClient({
            maxRetries: 0,
            clientId: "YOUR_CLIENT_ID",
            clientSecret: "YOUR_CLIENT_SECRET",
            environment: { candidApi: server.baseUrl, preEncounter: server.baseUrl },
        });

        const rawResponseBody = [
            {
                id: "id",
                organization_id: "organization_id",
                deactivated: true,
                version: 1,
                updated_at: "2024-01-15T09:30:00Z",
                updating_user_id: "updating_user_id",
                alternative_patient_mrn: "alternative_patient_mrn",
                primary_patient_mrn: "primary_patient_mrn",
            },
            {
                id: "id",
                organization_id: "organization_id",
                deactivated: true,
                version: 1,
                updated_at: "2024-01-15T09:30:00Z",
                updating_user_id: "updating_user_id",
                alternative_patient_mrn: "alternative_patient_mrn",
                primary_patient_mrn: "primary_patient_mrn",
            },
        ];

        server
            .mockEndpoint()
            .get("/patient-merge/v1/updates/scan")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.preEncounter.patientMerges.v1.scan({
            since: new Date("2024-01-15T09:30:00.000Z"),
        });
        expect(response).toEqual({
            body: [
                {
                    id: CandidApi.preEncounter.PatientMergeId("id"),
                    organizationId: CandidApi.preEncounter.OrganizationId("organization_id"),
                    deactivated: true,
                    version: 1,
                    updatedAt: new Date("2024-01-15T09:30:00.000Z"),
                    updatingUserId: CandidApi.preEncounter.UserId("updating_user_id"),
                    alternativePatientMrn: "alternative_patient_mrn",
                    primaryPatientMrn: "primary_patient_mrn",
                },
                {
                    id: CandidApi.preEncounter.PatientMergeId("id"),
                    organizationId: CandidApi.preEncounter.OrganizationId("organization_id"),
                    deactivated: true,
                    version: 1,
                    updatedAt: new Date("2024-01-15T09:30:00.000Z"),
                    updatingUserId: CandidApi.preEncounter.UserId("updating_user_id"),
                    alternativePatientMrn: "alternative_patient_mrn",
                    primaryPatientMrn: "primary_patient_mrn",
                },
            ],
            ok: true,
            headers: expect.any(Object),
            rawResponse: expect.any(Object),
        });
    });
});
