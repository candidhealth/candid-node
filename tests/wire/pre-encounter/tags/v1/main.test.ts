// This file was auto-generated by Fern from our API Definition.

import * as CandidApi from "../../../../../src/api/index";
import { CandidApiClient } from "../../../../../src/Client";
import { mockServerPool } from "../../../../mock-server/MockServerPool";
import { mockOAuthScheme } from "../../../mockAuth";

describe("V1Client", () => {
    test("get", async () => {
        const server = mockServerPool.createServer();
        mockOAuthScheme(server);

        const client = new CandidApiClient({
            maxRetries: 0,
            clientId: "YOUR_CLIENT_ID",
            clientSecret: "YOUR_CLIENT_SECRET",
            environment: { candidApi: server.baseUrl, preEncounter: server.baseUrl },
        });

        const rawResponseBody = {
            id: "id",
            organization_id: "organization_id",
            deactivated: true,
            version: 1,
            updated_at: "2024-01-15T09:30:00Z",
            updating_user_id: "updating_user_id",
            value: "value",
            description: "description",
            alert: true,
        };

        server.mockEndpoint().get("/tags/v1/id").respondWith().statusCode(200).jsonBody(rawResponseBody).build();

        const response = await client.preEncounter.tags.v1.get(CandidApi.preEncounter.TagId("id"));
        expect(response).toEqual({
            body: {
                id: CandidApi.preEncounter.TagId("id"),
                organizationId: CandidApi.preEncounter.OrganizationId("organization_id"),
                deactivated: true,
                version: 1,
                updatedAt: new Date("2024-01-15T09:30:00.000Z"),
                updatingUserId: CandidApi.preEncounter.UserId("updating_user_id"),
                value: "value",
                description: "description",
                alert: true,
            },
            ok: true,
            headers: expect.any(Object),
            rawResponse: expect.any(Object),
        });
    });

    test("getAll", async () => {
        const server = mockServerPool.createServer();
        mockOAuthScheme(server);

        const client = new CandidApiClient({
            maxRetries: 0,
            clientId: "YOUR_CLIENT_ID",
            clientSecret: "YOUR_CLIENT_SECRET",
            environment: { candidApi: server.baseUrl, preEncounter: server.baseUrl },
        });

        const rawResponseBody = {
            items: [
                {
                    id: "id",
                    organization_id: "organization_id",
                    deactivated: true,
                    version: 1,
                    updated_at: "2024-01-15T09:30:00Z",
                    updating_user_id: "updating_user_id",
                    value: "value",
                    description: "description",
                    alert: true,
                },
                {
                    id: "id",
                    organization_id: "organization_id",
                    deactivated: true,
                    version: 1,
                    updated_at: "2024-01-15T09:30:00Z",
                    updating_user_id: "updating_user_id",
                    value: "value",
                    description: "description",
                    alert: true,
                },
            ],
            next_page_token: "next_page_token",
            prev_page_token: "prev_page_token",
            total: 1,
        };

        server.mockEndpoint().get("/tags/v1").respondWith().statusCode(200).jsonBody(rawResponseBody).build();

        const response = await client.preEncounter.tags.v1.getAll();
        expect(response).toEqual({
            body: {
                items: [
                    {
                        id: CandidApi.preEncounter.TagId("id"),
                        organizationId: CandidApi.preEncounter.OrganizationId("organization_id"),
                        deactivated: true,
                        version: 1,
                        updatedAt: new Date("2024-01-15T09:30:00.000Z"),
                        updatingUserId: CandidApi.preEncounter.UserId("updating_user_id"),
                        value: "value",
                        description: "description",
                        alert: true,
                    },
                    {
                        id: CandidApi.preEncounter.TagId("id"),
                        organizationId: CandidApi.preEncounter.OrganizationId("organization_id"),
                        deactivated: true,
                        version: 1,
                        updatedAt: new Date("2024-01-15T09:30:00.000Z"),
                        updatingUserId: CandidApi.preEncounter.UserId("updating_user_id"),
                        value: "value",
                        description: "description",
                        alert: true,
                    },
                ],
                nextPageToken: CandidApi.preEncounter.PageToken("next_page_token"),
                prevPageToken: CandidApi.preEncounter.PageToken("prev_page_token"),
                total: 1,
            },
            ok: true,
            headers: expect.any(Object),
            rawResponse: expect.any(Object),
        });
    });

    test("create", async () => {
        const server = mockServerPool.createServer();
        mockOAuthScheme(server);

        const client = new CandidApiClient({
            maxRetries: 0,
            clientId: "YOUR_CLIENT_ID",
            clientSecret: "YOUR_CLIENT_SECRET",
            environment: { candidApi: server.baseUrl, preEncounter: server.baseUrl },
        });
        const rawRequestBody = { value: "value" };
        const rawResponseBody = {
            id: "id",
            organization_id: "organization_id",
            deactivated: true,
            version: 1,
            updated_at: "2024-01-15T09:30:00Z",
            updating_user_id: "updating_user_id",
            value: "value",
            description: "description",
            alert: true,
        };

        server
            .mockEndpoint()
            .post("/tags/v1")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.preEncounter.tags.v1.create({
            value: "value",
        });
        expect(response).toEqual({
            body: {
                id: CandidApi.preEncounter.TagId("id"),
                organizationId: CandidApi.preEncounter.OrganizationId("organization_id"),
                deactivated: true,
                version: 1,
                updatedAt: new Date("2024-01-15T09:30:00.000Z"),
                updatingUserId: CandidApi.preEncounter.UserId("updating_user_id"),
                value: "value",
                description: "description",
                alert: true,
            },
            ok: true,
            headers: expect.any(Object),
            rawResponse: expect.any(Object),
        });
    });

    test("update", async () => {
        const server = mockServerPool.createServer();
        mockOAuthScheme(server);

        const client = new CandidApiClient({
            maxRetries: 0,
            clientId: "YOUR_CLIENT_ID",
            clientSecret: "YOUR_CLIENT_SECRET",
            environment: { candidApi: server.baseUrl, preEncounter: server.baseUrl },
        });
        const rawRequestBody = { value: "value" };
        const rawResponseBody = {
            id: "id",
            organization_id: "organization_id",
            deactivated: true,
            version: 1,
            updated_at: "2024-01-15T09:30:00Z",
            updating_user_id: "updating_user_id",
            value: "value",
            description: "description",
            alert: true,
        };

        server
            .mockEndpoint()
            .put("/tags/v1/id/version")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.preEncounter.tags.v1.update(CandidApi.preEncounter.TagId("id"), "version", {
            value: "value",
        });
        expect(response).toEqual({
            body: {
                id: CandidApi.preEncounter.TagId("id"),
                organizationId: CandidApi.preEncounter.OrganizationId("organization_id"),
                deactivated: true,
                version: 1,
                updatedAt: new Date("2024-01-15T09:30:00.000Z"),
                updatingUserId: CandidApi.preEncounter.UserId("updating_user_id"),
                value: "value",
                description: "description",
                alert: true,
            },
            ok: true,
            headers: expect.any(Object),
            rawResponse: expect.any(Object),
        });
    });

    test("deactivate", async () => {
        const server = mockServerPool.createServer();
        mockOAuthScheme(server);

        const client = new CandidApiClient({
            maxRetries: 0,
            clientId: "YOUR_CLIENT_ID",
            clientSecret: "YOUR_CLIENT_SECRET",
            environment: { candidApi: server.baseUrl, preEncounter: server.baseUrl },
        });

        server.mockEndpoint().delete("/tags/v1/id/version").respondWith().statusCode(200).build();

        const response = await client.preEncounter.tags.v1.deactivate(CandidApi.preEncounter.TagId("id"), "version");
        expect(response).toEqual({
            body: undefined,
            ok: true,
            headers: expect.any(Object),
            rawResponse: expect.any(Object),
        });
    });
});
