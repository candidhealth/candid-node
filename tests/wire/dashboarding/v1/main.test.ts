// This file was auto-generated by Fern from our API Definition.

import { CandidApiClient } from "../../../../src/Client";
import { mockServerPool } from "../../../mock-server/MockServerPool";
import { mockOAuthScheme } from "../../mockAuth";

describe("V1Client", () => {
    test("query_metrics", async () => {
        const server = mockServerPool.createServer();
        mockOAuthScheme(server);

        const client = new CandidApiClient({
            maxRetries: 0,
            clientId: "YOUR_CLIENT_ID",
            clientSecret: "YOUR_CLIENT_SECRET",
            environment: { candidApi: server.baseUrl, preEncounter: server.baseUrl },
        });
        const rawRequestBody = [{ metric: "patient_total_balance_v1" }, { metric: "patient_total_balance_v1" }];
        const rawResponseBody = {
            results: [
                { metric: "patient_total_balance_v1", value: { key: "value" }, snapshot: "today", error: "error" },
                { metric: "patient_total_balance_v1", value: { key: "value" }, snapshot: "today", error: "error" },
            ],
        };

        server
            .mockEndpoint()
            .post("/api/dashboarding/v1/metrics")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.dashboarding.v1.queryMetrics([
            {
                metric: "patient_total_balance_v1",
            },
            {
                metric: "patient_total_balance_v1",
            },
        ]);
        expect(response).toEqual({
            body: {
                results: [
                    {
                        metric: "patient_total_balance_v1",
                        value: {
                            key: "value",
                        },
                        snapshot: "today",
                        error: "error",
                    },
                    {
                        metric: "patient_total_balance_v1",
                        value: {
                            key: "value",
                        },
                        snapshot: "today",
                        error: "error",
                    },
                ],
            },
            ok: true,
            headers: expect.any(Object),
            rawResponse: expect.any(Object),
        });
    });
});
