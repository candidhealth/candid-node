// This file was auto-generated by Fern from our API Definition.

import * as CandidApi from "../../../../src/api/index";
import { CandidApiClient } from "../../../../src/Client";
import { mockServerPool } from "../../../mock-server/MockServerPool";
import { mockOAuthScheme } from "../../mockAuth";

describe("V2Client", () => {
    test("create_availity_eligibility_check", async () => {
        const server = mockServerPool.createServer();
        mockOAuthScheme(server);

        const client = new CandidApiClient({
            maxRetries: 0,
            clientId: "YOUR_CLIENT_ID",
            clientSecret: "YOUR_CLIENT_SECRET",
            environment: { candidApi: server.baseUrl, preEncounter: server.baseUrl },
        });
        const rawRequestBody = {
            member_id: "member_id",
            payer_id: "payer_id",
            provider_npi: "provider_npi",
            date_of_service: "2024-01-15T09:30:00Z",
            service_type_codes: ["service_type_codes", "service_type_codes"],
        };
        const rawResponseBody = "d5e9c84f-c2b2-4bf4-b4b0-7ffd7a9ffc32";

        server
            .mockEndpoint()
            .post("/api/eligibility/v2/avality-eligibility-check")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.eligibility.v2.createAvailityEligibilityCheck({
            memberId: "member_id",
            payerId: "payer_id",
            providerNpi: "provider_npi",
            dateOfService: new Date("2024-01-15T09:30:00.000Z"),
            serviceTypeCodes: ["service_type_codes", "service_type_codes"],
        });
        expect(response).toEqual({
            body: CandidApi.eligibility.v2.EligibilityCheckId("d5e9c84f-c2b2-4bf4-b4b0-7ffd7a9ffc32"),
            ok: true,
            headers: expect.any(Object),
            rawResponse: expect.any(Object),
        });
    });

    test("submit_eligibility_check_availity", async () => {
        const server = mockServerPool.createServer();
        mockOAuthScheme(server);

        const client = new CandidApiClient({
            maxRetries: 0,
            clientId: "YOUR_CLIENT_ID",
            clientSecret: "YOUR_CLIENT_SECRET",
            environment: { candidApi: server.baseUrl, preEncounter: server.baseUrl },
        });

        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .get("/api/eligibility/v2/availity")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.eligibility.v2.submitEligibilityCheckAvaility();
        expect(response).toEqual({
            body: {
                key: "value",
            },
            ok: true,
            headers: expect.any(Object),
            rawResponse: expect.any(Object),
        });
    });

    test("submit_eligibility_check_availity_post", async () => {
        const server = mockServerPool.createServer();
        mockOAuthScheme(server);

        const client = new CandidApiClient({
            maxRetries: 0,
            clientId: "YOUR_CLIENT_ID",
            clientSecret: "YOUR_CLIENT_SECRET",
            environment: { candidApi: server.baseUrl, preEncounter: server.baseUrl },
        });
        const rawRequestBody = { key: "value" };
        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .post("/api/eligibility/v2/availity")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.eligibility.v2.submitEligibilityCheckAvailityPost({
            key: "value",
        });
        expect(response).toEqual({
            body: {
                key: "value",
            },
            ok: true,
            headers: expect.any(Object),
            rawResponse: expect.any(Object),
        });
    });

    test("find_availity_eligibility_results", async () => {
        const server = mockServerPool.createServer();
        mockOAuthScheme(server);

        const client = new CandidApiClient({
            maxRetries: 0,
            clientId: "YOUR_CLIENT_ID",
            clientSecret: "YOUR_CLIENT_SECRET",
            environment: { candidApi: server.baseUrl, preEncounter: server.baseUrl },
        });
        const rawRequestBody = {
            member_id: "member_id",
            payer_id: "payer_id",
            date_of_service: "2024-01-15T09:30:00Z",
            provider_npi: "provider_npi",
        };
        const rawResponseBody = {
            results: [
                {
                    id: "id",
                    dateOfService: "2024-01-15T09:30:00Z",
                    payerId: "payerId",
                    providerNPI: "providerNPI",
                    dependent: "dependent",
                    status: "ACTIVE",
                },
                {
                    id: "id",
                    dateOfService: "2024-01-15T09:30:00Z",
                    payerId: "payerId",
                    providerNPI: "providerNPI",
                    dependent: "dependent",
                    status: "ACTIVE",
                },
            ],
        };

        server
            .mockEndpoint()
            .post("/api/eligibility/v2/existing-checks")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.eligibility.v2.findAvailityEligibilityResults({
            memberId: "member_id",
            payerId: "payer_id",
            dateOfService: new Date("2024-01-15T09:30:00.000Z"),
            providerNpi: "provider_npi",
        });
        expect(response).toEqual({
            body: {
                results: [
                    {
                        id: "id",
                        dateOfService: new Date("2024-01-15T09:30:00.000Z"),
                        payerId: "payerId",
                        providerNpi: "providerNPI",
                        dependent: "dependent",
                        status: "ACTIVE",
                    },
                    {
                        id: "id",
                        dateOfService: new Date("2024-01-15T09:30:00.000Z"),
                        payerId: "payerId",
                        providerNpi: "providerNPI",
                        dependent: "dependent",
                        status: "ACTIVE",
                    },
                ],
            },
            ok: true,
            headers: expect.any(Object),
            rawResponse: expect.any(Object),
        });
    });

    test("get_by_id", async () => {
        const server = mockServerPool.createServer();
        mockOAuthScheme(server);

        const client = new CandidApiClient({
            maxRetries: 0,
            clientId: "YOUR_CLIENT_ID",
            clientSecret: "YOUR_CLIENT_SECRET",
            environment: { candidApi: server.baseUrl, preEncounter: server.baseUrl },
        });

        const rawResponseBody = {
            id: "id",
            dateOfService: "2024-01-15T09:30:00Z",
            payerId: "payerId",
            providerNPI: "providerNPI",
            dependent: "dependent",
            status: "ACTIVE",
        };

        server
            .mockEndpoint()
            .get("/api/eligibility/v2/d5e9c84f-c2b2-4bf4-b4b0-7ffd7a9ffc32")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.eligibility.v2.getById(
            CandidApi.eligibility.v2.EligibilityCheckId("d5e9c84f-c2b2-4bf4-b4b0-7ffd7a9ffc32"),
        );
        expect(response).toEqual({
            body: {
                id: "id",
                dateOfService: new Date("2024-01-15T09:30:00.000Z"),
                payerId: "payerId",
                providerNpi: "providerNPI",
                dependent: "dependent",
                status: "ACTIVE",
            },
            ok: true,
            headers: expect.any(Object),
            rawResponse: expect.any(Object),
        });
    });
});
