// This file was auto-generated by Fern from our API Definition.

/**
 * The point in time a metric value is read from. `today` is current state. `yesterday` is the
 * previous nightly snapshot, and is only supported on the four count metrics —
 * `patient_outstanding_balance_count_v1`, `pre_adjudication_claims_count_v1`,
 * `post_adjudication_claims_count_v1`, and `payment_posting_claims_count_v1`. Requesting
 * `yesterday` for any other metric returns an `error` on that result.
 */
export const MetricSnapshot = {
    Today: "today",
    Yesterday: "yesterday",
} as const;
export type MetricSnapshot = (typeof MetricSnapshot)[keyof typeof MetricSnapshot];
